services:
  bot:
    build: .
    env_file:
      - .env
    ports:
      - "${HTTP_PORT:-8080}:${HTTP_PORT:-8080}"
    volumes:
      - ./storage:/app/storage
      - ./logs:/app/logs
      - downloads:/tmp/downloads
      - ./config:/app/config
    restart: unless-stopped

volumes:
  downloads:
